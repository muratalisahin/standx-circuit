# StandX Circuit

Watch the engine. DUSD in the middle, every market as a module.

Live StandX schematic: volume as flow, open interest as node size, funding as pulse, order book as an x-ray. Protocol map for SIP-1 through SIP-5 — not a quiz, not a health score.

```bat
npm install
npm run dev
```

Open the `Local:` URL Vite prints.

Educational visualization only. Not investment advice. Not a vault explorer — live numbers come from StandX public market and depth APIs only.
